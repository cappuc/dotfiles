# Tracks skills installed by `npx skills` without syncing their installed copies.
# Restore this lockfile, then use the skills CLI to reinstall the recorded skills.
[application]
name = agent-skills

[configuration_files]
.agents/.skill-lock.json
