# Not supported by mackup upstream. Only the hand-edited config is synced:
# env.sh and shell-hook.zsh are regenerated by the lean-ctx installer.
# Paths are relative to $XDG_CONFIG_HOME (~/.config).
[application]
name = lean-ctx

[xdg_configuration_files]
lean-ctx/config.toml
lean-ctx/layout.toml
